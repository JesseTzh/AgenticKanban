services:
  agentic-kanban-app:
    image: jessetzh/agentic-kanban:latest
    container_name: agentic-kanban-app
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      UPLOAD_STORAGE: local
      UPLOAD_DIR: /app/data/uploads
    restart: unless-stopped
