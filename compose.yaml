services:
  agentic-kanban-app:
    image: jessetzh/agentic-kanban:latest
    container_name: agentic-kanban-app
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
